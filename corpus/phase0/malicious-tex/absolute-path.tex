% Synthetic adversarial input. The harness replaces the placeholder only in its random job directory.
% FORMULABRIDGE_ATTACK_RESULT: attack-result.txt
\documentclass{article}
\directlua{
  local openOk, handle = pcall(io.open, [[@@ABSOLUTE_CANARY@@]], "rb")
  if not openOk then handle = nil end
  local result = assert(io.open("attack-result.txt", "wb"))
  result:write(handle and "escaped" or "blocked")
  if handle then handle:close() end
  result:close()
}
\begin{document}absolute path probe\end{document}
