% Synthetic adversarial input. The Job Object memory ceiling must stop allocation below the fixed limit.
% FORMULABRIDGE_ATTACK_RESULT: no marker is expected before memory pressure terminates LuaLaTeX.
\documentclass{article}
\directlua{
  local chunks = {}
  while true do
    table.insert(chunks, string.rep("x", 16 * 1024 * 1024))
  end
}
\begin{document}memory exhaustion probe\end{document}
