% Synthetic adversarial input. The parent harness poisons TEXINPUTS before launch.
% FORMULABRIDGE_ATTACK_RESULT: attack-result.txt
\documentclass{article}
\directlua{
  local candidate = kpse.find_file("outside-canary.txt", "tex")
  local handle = candidate and io.open(candidate, "rb") or nil
  local result = assert(io.open("attack-result.txt", "wb"))
  result:write((candidate or handle) and "escaped" or "blocked")
  if handle then handle:close() end
  result:close()
}
\begin{document}search path probe\end{document}
