# FormulaBridge

FormulaBridge 的领域语言描述由 LaTeX 驱动、在 Microsoft Word 中管理和呈现的公式，以及它们使用的本地 TeX 配置。

## 产品边界

**首要用户**：
已经安装 TeX，并使用现代 Windows Word 撰写论文或技术文档的个人科研用户。
_Avoid_：同时把企业管理员、Aurora 迁移用户称为首要用户

**FormulaBridge 1.0**：
可以正式使用的核心版本，包含任务窗格、三类公式、插入与编辑、双渲染、多渲染配置、基本编号与引用、源码恢复、诊断和签名安装。
_Avoid_：完整版本、技术预览

**正式发行版**：
通过 FormulaBridge 官方渠道提供的闭源桌面产品和签名安装包；企业集中部署不属于 1.0 的首发渠道。
_Avoid_：开源版本、Office 商店加载项

**核心首发**：
不以账号、联网激活或商业授权系统为发布前提的 FormulaBridge 1.0；架构可以预留授权边界，但商业模式另行决策。
_Avoid_：免费版、在线激活版

**前置可行性门槛**：
在完整功能开发前必须以样机通过的 VSTO 用户级自动加载、普通复制源码携带、SVG/PNG Word 往返和 TeX 文件隔离验证。
_Avoid_：后期技术验证、功能验收

**纵向产品闭环**：
风险样机之后首先交付的“安装、功能区、任务窗格、编辑、OMML/TeX、写入、保存重开再编辑”完整路径；1.0 功能只能在此闭环上扩展。
_Avoid_：前端原型、解析器演示

**1.1 编辑扩展**：
在 FormulaBridge 1.0 之后交付的浮动编辑器和章节感知编号；它们不阻塞任务窗格与基本编号构成的 1.0。
_Avoid_：1.0 首发功能、完整编辑器

**支持文档**：
FormulaBridge 1.0 正式承诺兼容的 `.docx` 和 `.docm` Word 文档；模板格式不属于 1.0 支持范围。
_Avoid_：把 `.dotx`、`.dotm` 称为 1.0 支持文档

**可写文档状态**：
支持文档处于普通可编辑状态，且未开启修订、未处于实时共同编辑、未受只读或签名保护；只有这种状态允许 FormulaBridge 修改文档。
_Avoid_：可编辑文档、已打开文档

**格式转换副本**：
从 `.doc`、`.rtf`、`.odt` 或模板另存得到的新 `.docx` 或 `.docm` 文件；FormulaBridge 不在原格式中承诺管理公式。
_Avoid_：原文档升级、原地转换

## 公式

**FormulaBridge 公式**：
由稳定标识和权威 LaTeX 源码定义、由 FormulaBridge 管理的 Word 文档对象。
_Avoid_：OLE 公式、图片公式、普通 Word 公式

**LaTeX 源码**：
FormulaBridge 公式的权威语义来源；OMML、SVG 和 PNG 都是由它生成的表示。
_Avoid_：把可见 Word 内容与 LaTeX 源码同时称为事实来源

**可见表示**：
FormulaBridge 公式在 Word 中显示的 OMML，或由本地 TeX 生成并嵌入的 SVG 与 PNG；它是 LaTeX 源码的派生投影。
_Avoid_：最终源码、事实来源

**分歧公式**：
可见表示在 FormulaBridge 之外被修改，导致它与权威 LaTeX 源码不再一致的 FormulaBridge 公式。
_Avoid_：已同步公式、自动合并公式

**无损 OMML 反向导入**：
仅当 Core 能证明 Word OMML 可完整表达为受支持 LaTeX 时，经过差异预览创建新权威源码的恢复操作；图片公式和不支持的 OMML 不做猜测或 OCR。
_Avoid_：以 Word 内容为准、自动反向转换

**公式身份**：
由 UUID 表示的单个公式实例身份；移动保持身份，复制创建新身份。
_Avoid_：把复制品与原公式视为同一实例

**公式标签**：
用户为编号和交叉引用指定的文档内唯一名称；复制公式时默认清除。
_Avoid_：UUID、显示编号

**标签冲突**：
用户提交了文档中已有的公式标签；FormulaBridge 阻止提交并定位冲突公式，不自动重命名任一标签。
_Avoid_：重复标签、自动编号标签

**身份冲突**：
多个可见公式共享同一 UUID 的诊断状态；打开文档只报告，显式修复保留一个规范实例，并为其余实例分配新身份和清除冲突标签。
_Avoid_：复制公式、标签冲突

**基本编号**：
FormulaBridge 1.0 提供的文档内连续公式编号，不包含按标题级别重置的章节编号。
_Avoid_：章节编号、静态编号文本

**字段编号**：
使用 Word 原生 `SEQ`、`REF` 字段和书签实现的基本编号与引用；局部结构变化在同一撤销操作中更新受影响范围，全文更新由用户明确触发。
_Avoid_：静态编号、FormulaBridge 自绘编号

**公式引用**：
由 Word 原生字段指向公式标签的可更新交叉引用。
_Avoid_：手写编号、静态引用文本

**源码可移植复制**：
普通 Word 复制粘贴保留公式源码和必要元数据，同时创建新公式身份并清除公式标签。
_Avoid_：仅复制可见表示、专用复制命令

**可复制元数据载体**：
经受支持 Word 版本实测、能够随普通公式对象复制的一个或多个 OOXML 载体；Custom XML 只是候选实现而不是产品契约。
_Avoid_：Custom XML 复制、专用剪贴板格式

**文档内源码**：
以明文 Custom XML 保存在 Word 文档包中的权威 LaTeX 源码，其保密性由 Word 文档本身的加密和访问控制提供。
_Avoid_：本机源码、FormulaBridge 加密源码

**未映射公式**：
文档环境要求尚未映射到本机兼容渲染配置的公式；其嵌入表示和源码可查看，但不能重新编译或覆盖。
_Avoid_：自动换环境公式、损坏公式

**待更新公式**：
本机 TeX 安装、宏包、字体或渲染配置发生身份变化后仍显示既有嵌入表示、等待用户明确重新渲染的公式。
_Avoid_：未映射公式、自动更新公式

**受管理引用**：
由 FormulaBridge 创建并可在标签重命名时原子更新的同文档 Word 交叉引用。
_Avoid_：手写引用、任意 Word 字段

**双格式表示**：
本地 TeX 路径共同嵌入并经过 Word 保存、重开、复制、打印和 PDF 导出验证的 SVG 与 PNG fallback。
_Avoid_：外链图片、仅 SVG、仅 PNG

**已脱离管理公式**：
可见表示仍然存在但源码元数据丢失、重复或损坏的公式；FormulaBridge 保留其内容并提供恢复、重新录入或重新关联。
_Avoid_：损坏公式、自动重建公式

**孤立元数据**：
找不到对应可见公式对象的文档内记录；打开文档只报告，只有用户明确执行修复或确认保存清理时才可撤销地删除。
_Avoid_：无效数据、自动清理项

**目标样式适配**：
跨文档粘贴默认保留公式字号、颜色和显示方式，同时采用目标段落布局；用户可以显式执行“匹配目标样式”。
_Avoid_：完全保留格式、自动匹配全部格式

**公式辅助说明**：
由 FormulaBridge AST 生成并清理的 SVG 公式可读文本或 MathML 语义；无法可靠生成时明确标记并允许用户编辑。
_Avoid_：完整 LaTeX 源码、图片文件名

**自动渲染**：
仅在 Core 明确证明语义可无损表达时选择 OMML，否则选择本地 TeX 的渲染策略。
_Avoid_：试错转换、静默降级

## TeX 配置

**TeX 安装**：
本机实际存在的一套 TeX Live、MiKTeX 或便携式 TeX 发行版及其版本和根目录。
_Avoid_：TeX 环境

**渲染配置**：
一个 TeX 安装、一个引擎、转换器、preamble 和安全策略的明确组合。
_Avoid_：引擎环境、环境配置、TeX 环境

**首发引擎**：
FormulaBridge 1.0 正式支持并分别验证的 `latex`、`pdflatex`、`xelatex` 和 `lualatex` 本地执行路径。
_Avoid_：任意 TeX 引擎、未经测试的兼容引擎

**TeX 支持窗口**：
滚动正式支持当前及前两个年度版 TeX Live 和当前稳定 MiKTeX；更旧发行版与自定义工具链属于尽力兼容。
_Avoid_：所有 TeX 版本、单一 TeX 版本

**用户批准工具链**：
由本机用户显式选择绝对路径并通过版本与最小渲染健康检查的引擎和转换器；文件身份变化会使批准失效，文档不能指定程序。
_Avoid_：自动发现工具链、文档工具链

**无 TeX 模式**：
未映射任何可用渲染配置时仍可创建和编辑 OMML 公式、查看嵌入表示与源码，但不能编译或覆盖需要本地 TeX 的公式。
_Avoid_：禁用插件、自动安装 TeX

**文档环境要求**：
Word 文档声明的引擎、宏包和渲染能力要求，不包含任何本机绝对路径。
_Avoid_：文档 TeX 路径、文档环境

**公式覆盖配置**：
单个 FormulaBridge 公式对文档默认渲染配置的显式覆盖。
_Avoid_：单公式环境、临时环境

**兼容映射**：
用户把文档环境要求明确关联到本机渲染配置的关系；映射可以满足能力要求但不保证像素级复现。
_Avoid_：静默换引擎、自动环境替换

**preamble 层**：
按“渲染配置、文档、公式附加项”顺序显式组合的 preamble 来源；重复选项或定义冲突属于错误。
_Avoid_：隐式覆盖、公式完整 preamble

**渲染缓存**：
有容量和期限限制的本地派生数据，只包含清理后的 SVG/PNG、尺寸信息和不可逆哈希，不包含明文源码或完整日志。
_Avoid_：源码历史、公式草稿

**缺失宏包诊断**：
指出缺失宏包、对应 TeX 安装及其 TeX Live 或 MiKTeX 安装方法的可执行建议；FormulaBridge 1.0 不代替用户安装宏包。
_Avoid_：自动包安装、原始编译日志

## 交换与恢复

**首发交换能力**：
FormulaBridge 1.0 支持 LaTeX 文本粘贴与复制，以及当前公式的 LaTeX、SVG 和 PNG 导出。
_Avoid_：把完整 OMML、MathML、PDF 导入导出称为 1.0 功能

**公式片段**：
保存在本机的轻量用户条目，包含名称、LaTeX 和可选说明，不依赖账号、云同步或团队数据库。
_Avoid_：团队模板、云模板库

**受保护草稿**：
使用当前 Windows 用户保护机制加密、有明确有效期且可禁用或清除的未提交 LaTeX 编辑内容；它不属于渲染缓存。
_Avoid_：明文临时源码、渲染缓存

**完整脱敏日志**：
保留该次 TeX 作业诊断上下文但移除用户名、用户路径和其他敏感信息的界面日志；原始日志仅在用户明确授权后临时导出。
_Avoid_：原始日志、摘要错误码

**兼容元数据**：
具有明确版本和迁移规则的文档内公式数据；旧版程序遇到更高版本时只读源码和可见表示，不能降级写回。
_Avoid_：当前版本数据、尽力解析数据

**确认式元数据迁移**：
新版本先只读验证旧架构，在用户首次确认写入时迁移，并保留旧记录直到成功保存和重开验证；重大版本迁移提示另存副本。
_Avoid_：打开即迁移、静默升级

**文档会话**：
绑定到单个 Word 文档窗口的编辑器状态、文档配置和待处理操作；不同窗口之间不得共享当前公式或隐式渲染配置。
_Avoid_：全局编辑会话、Word 进程会话

**原子 Word 操作**：
作为一个可理解的 Word 撤销记录完成的插入、更新、类型转换、编号或明确分组的批量修改；失败或取消不留下部分写入。
_Avoid_：COM 写入步骤、不可撤销操作

**两阶段批量更新**：
先对明确范围完成验证和渲染，再由用户选择全部取消或将成功项按可审计、可撤销的分组写入；失败项不得被静默跳过。
_Avoid_：边编译边写入、尽力批量更新

**未保存支持文档**：
尚无文件路径但将在首次保存时使用 `.docx` 或 `.docm` 的新 Word 文档；公式元数据先存在内存文档包中，并由受保护草稿兜底。
_Avoid_：临时文档、无路径文件

**可控卸载**：
绝不修改用户文档，删除程序、缓存和普通日志，并由用户选择是否保留设置、渲染配置和受保护草稿的卸载行为。
_Avoid_：完全清理、保留全部数据

## 信任边界

**文档 TeX 输入**：
从 Word 文档读取的 LaTeX 源码，一律视为不可信输入；FormulaBridge 不自动编译，只有用户明确触发时才在受限 RenderHost 中处理。
_Avoid_：可信文档公式

**明确编译操作**：
用户在编辑器中开始修改当前公式，或在确认范围和渲染配置后执行批量更新；仅打开文档或选择公式不属于明确编译操作。
_Avoid_：打开即编译、选择即编译

**受控 TeX 根**：
单次编译唯一允许读取的 TeX 安装目录和随机任务目录；FormulaBridge 1.0 不允许源码直接读取其他用户文件路径。
_Avoid_：文档目录、用户授权目录、任意绝对路径

**受保护文档**：
只读、受保护视图或具有数字签名的 Word 文档；FormulaBridge 可以查看其公式和源码，但不能绕过保护进行修改。
_Avoid_：可编辑文档

**用户级安装**：
不静默提权、仅为当前 Windows 用户安装的 FormulaBridge 本体；需要管理员权限的缺失先决条件由独立签名引导程序处理。
_Avoid_：机器级安装、自动提权安装

**Word 支持窗口**：
持续验证 Microsoft 365 Current Channel、Monthly Enterprise Channel 的当前受支持版本，以及 Office 2024 x64 的滚动兼容范围。
_Avoid_：单一 Word 构建、所有桌面 Word

**不可放宽安全上限**：
文档和公式不能提高的单次编译限制；初始值为输入 256 KiB、交互 30 秒、批量单项 120 秒、内存 1 GiB、最多 64 个输出文件且合计 64 MiB，并由产品版本依据受控语料调整。
_Avoid_：用户上限、文档编译设置

**一致性更新**：
在所有 Word 实例和 RenderHost 退出后，由用户确认安装并作为兼容组件集合整体升级或回滚的更新。
_Avoid_：热更新、组件独立替换

**更新信任链**：
由安装器内置信任根验证更新清单与包的签名、哈希、版本和防降级规则，并支持密钥轮换与撤销；验证失败不得安装。
_Avoid_：HTTPS 下载、文件名校验

**选择加入更新**：
稳定通道为默认、预览通道需明确加入并提示风险的更新体验；联网检查默认关闭，立即检查或周期检查由用户启用，安装始终再次确认。
_Avoid_：自动更新、默认预览通道

**发布验证语料**：
版本化、无隐私数据且可重复运行的公式与 Word 文档集合，覆盖四种引擎、专业宏包、中文、辅助说明、复制、千公式和恶意 TeX 场景。
_Avoid_：临时测试公式、用户文档

**本地保留策略**：
渲染缓存默认 1 GiB/30 天 LRU、脱敏日志 20 MiB/14 天、受保护草稿 7 天的本地数据规则；用户可缩短、禁用或立即清除。
_Avoid_：永久保留、退出即清理

**不可豁免发布阻断项**：
任何已知的文档损坏或数据丢失、静默数学语义变化、TeX 隔离逃逸、未授权代码执行、Word 崩溃、签名或更新验证绕过，以及未通过的前置验证或发布矩阵。
_Avoid_：已知限制、发布后修复项

**外部诊断与修复**：
Word 之外的 FormulaBridge 工具，用于检查加载注册、Office 位数、签名、VSTO/WebView2 运行时和禁用状态，并提供不绕过组织策略的修复建议。
_Avoid_：强制启用插件、注册表教程

**先决条件降级状态**：
缺少 VSTO 或 WebView2 运行时等组件时保留可用的原生状态说明，禁用编辑并引导用户运行独立签名安装程序，绝不静默提权。
_Avoid_：启动崩溃、自动提权修复

**VBA 不透明性**：
FormulaBridge 对 `.docm` 中既有 VBA 项目不读取、不执行、不修改，并通过保存重开测试验证其保持性。
_Avoid_：VBA 集成、宏安全扫描

**本地调用凭证**：
由当前用户 Pipe ACL、每次 RenderHost 启动的随机能力令牌和协议握手共同建立的调用权限；令牌不进入文档、配置或日志。
_Avoid_：固定密码、仅用户 ACL

**迁移助手**：
FormulaBridge 1.x 中由用户显式启动、独立实现并始终输出新文档和审计报告的 Aurora 文档转换工具；真实语料与法律审核是正式发布门槛。
_Avoid_：自动迁移、Aurora 兼容层

**字体要求**：
渲染配置声明的字体能力；允许时将字形转换为 SVG 路径，否则依靠嵌入 PNG fallback，并提示跨机器视觉差异而不复制用户字体文件。
_Avoid_：嵌入字体、像素一致保证
