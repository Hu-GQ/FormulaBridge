{
  "name": "formulabridge",
  "version": "0.1.0",
  "private": true,
  "description": "An independently developed LaTeX equation editor for Microsoft Word.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check tests/product-design.test.js && node --check tests/technical-solution.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
